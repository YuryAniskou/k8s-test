apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: dashboard-ingress
spec:
  rules:
    - host: our-trail.com
      http:
        paths:
          - backend:
            serviceName: mongo-express-service
            servicePort: 80
