apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: dashboard-ingress
  namespace: k8s-dashboard
spec:
  rules:
    - host: our-trail.com
      http:
        paths:
          - backend:
